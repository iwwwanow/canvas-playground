const CANVAS_ID = "channels-canvas";
const IMAGE_SRC = "assets/flowers.jpg";

const drawChannelsProject = (img) => {
  const composition = new Composition(CANVAS_ID, IMAGE_SRC);
  composition.init();
};
