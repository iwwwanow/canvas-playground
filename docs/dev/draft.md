- toast
  - полноценный редактор
  - простой скрипт
  - у тоста есть собственный фронт
    - обязательным ли он должен быть?
  - у тоста есть собственный бэк
    - обязательным ли он должен быть?
  - тосту нужны следующие контракты:
    - контракт входных данных
    - контракт взаимодействия фронта и бэка
    - контракт выходных данных
- toast-sequence
  - тосты можно слепливать в последовательности, если у них сходится входной и выходной тип
- начинать мне нужно с самого простого тоста их всех возможных
  - наверное просто изменение цвета по hue?
- svelte kit
- сначала сделать это все в одном файле, затем абстрагиировать
  - вынести фронт в модуль
  - вынести бэк в модуль
  - подсоединить это все в core

# later:

- is desktop needed?
  - tairi

# open-questions:

- how to contract frontend?

```
Самый практичный путь: HTML + frontmatter/JSON schema. Автор пишет HTML-шаблон, в начале файла объявляет что принимает:

---
inputs:
  title: string
  duration: number
  color: hex
---
<div style="color: {{color}}">{{title}}</div>
```
