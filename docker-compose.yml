services:
  web:
    image: oven/bun:1
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
      - /app/packages/web/node_modules
    ports:
      - "5173:5173"
    command: sh -c "bun install && bun run web:dev"
