{
  "name": "xtc-toaster",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "trustedDependencies": [
    "better-sqlite3",
    "canvas",
    "esbuild"
  ],
  "scripts": {
    "typecheck": "tsc -p packages/lib/tsconfig.json && tsc -p packages/toasts/tsconfig.json",
    "web:dev": "bun --filter @xtc-toaster/web dev",
    "toast-1": "bun packages/toasts/src/toasts/toast-1_degas/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
