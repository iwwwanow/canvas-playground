{
  "name": "xtc-toaster",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "trustedDependencies": [
    "better-sqlite3",
    "esbuild"
  ],
  "scripts": {
    "web:dev": "bun --filter @xtc-toaster/web dev"
  }
}
